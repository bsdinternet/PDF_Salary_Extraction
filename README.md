# PDF_Salary_Extraction
This Python tool automates the extraction and calculation of per-day salary data from salary slip PDFs generated by PagarBook — a popular payroll platform used by Indian SMEs. Instead of manually reading each PDF and computing deductions, this tool reads the PDF, extracts attendance and earnings data, and produces a clean Excel report instantly.
